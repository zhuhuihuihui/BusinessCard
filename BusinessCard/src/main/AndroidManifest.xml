<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.businesscard"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="10" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.businesscard.WelcomeActivity"
            android:label="@string/activityLabel_welcome" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.LoginActivity"
            android:label="@string/activityLabel_login" >
        </activity>

        <activity
            android:name=".ui.MainActivity"
            android:label="@string/activityLabel_main" >
        </activity>

        <activity
            android:name=".ui.CreateMyCardFragment"
            android:label="@string/activityLabel_main" >
        </activity>

    </application>

</manifest>
